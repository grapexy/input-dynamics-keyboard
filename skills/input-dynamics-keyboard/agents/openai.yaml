interface:
  display_name: "Input Dynamics Keyboard"
  short_description: "Install and run Android IME logging"
  default_prompt: "Use $input-dynamics-keyboard to install the APK and run a local Android input dynamics session over ADB."
