interface:
  display_name: "Input Dynamics Keyboard"
  short_description: "Run Android IME logging with CLI"
  default_prompt: "Use $input-dynamics-keyboard to install the APK and run a local Android input dynamics session with the host CLI."
